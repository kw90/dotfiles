#!/bin/bash

export PKGMN=apt # Package Manager
export PKGOPT=
export PKGU=install --only-upgrade -y # Updating Options
export PKGI=install -y  # Installation Options
export PKGR=remove -y # Remove Options
